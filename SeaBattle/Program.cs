using SeaBattle.Menu;

class Program
{
    //todo цикл(внутри сколько кораблей у игрока осталось, сколько кораблей врага убито) setCursorPosition // if каждый сивол = X || O => осталось, если нет то 
    static void Main()
    {
        Menu menu = new Menu();
        menu.ShowMenu();
    }
}